"""Web-app version of the realtime motion-to-music demo.

Serves a browser-based client over HTTP and runs the GPU pipeline on
the same TCP port. Entry point: ``python -m demos.realtime_motion_graph_web``.
"""
